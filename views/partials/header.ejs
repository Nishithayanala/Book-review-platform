<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Book Review Platform</title>
    <!-- Link CSS here -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<header>
    <h1>Book Review Platform</h1>

    <nav class="main-nav">
        <!-- Centered links: Home + Admin -->
        <div class="nav-center">
            <% if (user) { %>
                <a href="/books" class="home-link">Home</a>
                <% if (user.isAdmin) { %>
                    <a href="/books/add">Add Book</a>
                    <a href="/books/admin/reviews/pending">Pending Reviews</a>
                    <a href="/books/manage">Manage Books</a>
                <% } %>
            <% } %>
        </div>

        <!-- Right: Logout -->
        <div class="nav-right">
            <% if (user) { %>
                <a href="/auth/logout" class="logout-btn">Logout</a>
            <% } %>
        </div>
    </nav>
    <hr>
</header>

</body>
